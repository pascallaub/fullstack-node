# Node.js / npm / yarn
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json # Optional: Often committed, but can be ignored if desired
yarn.lock         # Optional: Often committed, but can be ignored if desired

# Backend specific (if not covered by global node_modules)
backend/node_modules/

# Frontend specific (Vite / React)
vite-project/node_modules/
vite-project/dist/
vite-project/.env*
vite-project/.eslintcache

# Build output directories (general)
build/
dist/
out/

# Environment variables
.env
.env.*
!.env.example # Keep example env files

# IDE / Editor specific files
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Operating System generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Log files
logs/
*.log

# Helm values mit sensiblen Daten
values-prod.yaml
values-production.yaml
values-staging.yaml
*-values.yaml
*-secrets.yaml

# Kubernetes Konfigurationsdateien mit Credentials
*-config.yaml
kubeconfig*
*.kubeconfig

# Backup-Dateien von Editoren (können sensible Daten enthalten)
*.bak
*.backup
*.tmp
*~

secrets.yaml
*-secrets.yaml
secret*.yaml